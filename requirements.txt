# =============================================================================
# Divine Agent System — Python Dependencies
# Pinned for the May 2026 ecosystem (Python 3.12 baseline, 3.13 supported)
#
# Notes:
#   * Stdlib modules (asyncio, os, sys, pathlib, sqlite3, math, random,
#     statistics, concurrent.futures, multiprocessing, threading) are NOT
#     listed here — they ship with CPython.
#   * Experimental / fictional packages from the original spec
#     (neurosymbolic, cognitive-architectures, qiskit-ibmq-provider) have been
#     removed. qiskit-ibmq-provider was retired in 2024 — use `qiskit-ibm-runtime`.
#   * Versions reflect the active ecosystem as of 2026-05-14.
# =============================================================================

# ---- Core async & HTTP ------------------------------------------------------
aiohttp>=3.11.0,<4.0
aiofiles>=24.1.0
httpx>=0.28.0
requests>=2.32.0
websockets>=13.0
uvloop>=0.21.0; sys_platform != "win32"

# ---- Validation & typing ----------------------------------------------------
pydantic>=2.10.0,<3.0
pydantic-settings>=2.7.0
dataclasses-json>=0.6.7
typing-extensions>=4.12.0

# ---- Date / time / serialization -------------------------------------------
python-dateutil>=2.9.0
pytz>=2025.1
orjson>=3.10.0
PyYAML>=6.0.2
tomli>=2.0.2; python_version < "3.11"

# ---- Cryptography & auth ----------------------------------------------------
cryptography>=44.0.0
bcrypt>=4.2.0
PyJWT>=2.10.0

# ---- Database connectors ----------------------------------------------------
psycopg[binary]>=3.2.0           # psycopg3 — supersedes psycopg2
SQLAlchemy>=2.0.36
asyncpg>=0.30.0
redis>=5.2.0

# ---- Cloud SDKs (kept as extras-recommended) --------------------------------
boto3>=1.36.0
azure-identity>=1.19.0
azure-storage-blob>=12.24.0
google-cloud-storage>=2.19.0

# ---- Container / orchestration ---------------------------------------------
kubernetes>=31.0.0
docker>=7.1.0

# ---- Observability ----------------------------------------------------------
prometheus-client>=0.21.0
opentelemetry-api>=1.29.0
opentelemetry-sdk>=1.29.0
opentelemetry-exporter-otlp>=1.29.0
opentelemetry-instrumentation>=0.50b0
structlog>=24.4.0
loguru>=0.7.3
rich>=13.9.0

# ---- LLM / agent orchestration (the real "Supreme" stack) ------------------
langgraph>=0.4.0
langchain>=0.3.10
langchain-core>=0.3.20
langchain-community>=0.3.10
langchain-openai>=0.2.10
langchain-anthropic>=0.3.0
openai>=1.60.0
anthropic>=0.40.0

# ---- Model Context Protocol & Agent-to-Agent --------------------------------
# MCP became the de-facto agent-tooling standard in 2025; A2A followed.
mcp>=1.2.0

# ---- Vector / memory layer --------------------------------------------------
pinecone-client>=5.0.0
supabase>=2.10.0
chromadb>=0.5.20

# ---- Message queues ---------------------------------------------------------
aiokafka>=0.12.0
pika>=1.3.2

# ---- Data processing --------------------------------------------------------
numpy>=2.1.0
pandas>=2.2.3
scipy>=1.14.0
pyarrow>=18.0.0

# ---- Quantum computing (real libraries, current APIs) ----------------------
# Qiskit 1.x dropped `qiskit.execute` and `Aer` top-level imports —
# code must use qiskit_aer + transpile/run primitives.
qiskit>=1.3.0,<2.0
qiskit-aer>=0.15.0
qiskit-ibm-runtime>=0.34.0
cirq>=1.4.0
pennylane>=0.39.0

# ---- ML (optional, install via [ml] extra in practice) ---------------------
scikit-learn>=1.6.0
# Heavy frameworks left to the [ml] extra to keep base install lean:
#   tensorflow, torch, transformers

# ---- API server -------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
starlette>=0.41.0

# ---- Workflow / graphs ------------------------------------------------------
networkx>=3.4.0

# ---- Caching ----------------------------------------------------------------
cachetools>=5.5.0

# ---- Config & env -----------------------------------------------------------
python-dotenv>=1.0.1

# ---- Validation utilities ---------------------------------------------------
marshmallow>=3.23.0
cerberus>=1.3.5

# ---- Performance / introspection -------------------------------------------
psutil>=6.1.0

# ---- Testing ----------------------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
pytest-mock>=3.14.0

# ---- Dev tooling (kept here for convenience; can move to setup.py extras) --
ruff>=0.8.0
black>=24.10.0
mypy>=1.13.0
